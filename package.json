{
  "name": "nomoresoyface",
  "version": "1.2.0",
  "description": "A local-first Chrome extension that blurs distracting YouTuber faces and people in thumbnails.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "node scripts/hydrate-assets.mjs",
    "prepare-assets": "node scripts/hydrate-assets.mjs",
    "build": "npm run build:chrome",
    "build:chrome": "npm run prepare-assets && node scripts/build-chrome.mjs",
    "check": "node --check background.js && node --check content.js && node --check offscreen.js && node --check popup.js"
  },
  "dependencies": {
    "@tensorflow-models/coco-ssd": "2.2.3",
    "@vladmandic/face-api": "1.7.13"
  }
}
